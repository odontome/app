<% flash.each do |key, msg| 
	case key
    when :notice then key = "success"
    when :error then key = "danger"
    when :alert then key = "warning"
    end %>

	<div class="alert alert-<%= key %>">
		<%= msg %>
	</div>    
<% end %>