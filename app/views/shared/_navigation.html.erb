<!-- navbar -->
<header class="navbar-inverse navbar-fixed-top" role="banner">
    <div class="navbar-header">
        <button class="navbar-toggle" type="button" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <%= link_to (image_tag_with_at2x "logofull.png", :title => t(:home), :width => 150), root_path, :class => "navbar-brand" %>
    </div>
    <nav class="collapse navbar-collapse">
        <ul class="nav navbar-nav pull-right-lg">
            <% if current_user %> 
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <%= t(:datebooks) %>
                        <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                        <% @datebooks.each do |datebook| %>
                            <li><%= link_to is_current_datebook?(datebook.id) ? "#{datebook.name} ✓" : datebook.name , datebook %></li>
                        <% end %>
                        <% if user_is_admin? %>
                            <li class="divider"></li>
                            <li><%= link_to t(:create_new_datebook), new_datebook_url %></li>
                        <% end %>
                    </ul>
                </li>        
                <li><%= link_to t(:patients), patients_url %></li> 
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><img src="<%= avatar_url(current_user.email, 24)%>" alt="contact" class="img-circle avatar" />
                    <%= current_user.firstname %> <b class="caret"></b></a>

                    <ul class="dropdown-menu">
                        <li class="dropdown-header"><%= t(:manage) %></li>
                        <% if user_is_admin? %>
                            <li><%= link_to t(:datebooks), datebooks_url %></li>
                        <% end %>
                        <li><%= link_to t(:doctors), doctors_url %></li>
                        <li><%= link_to t(:treatments), treatments_url %></li>                        
                        <% if user_is_admin? %>
                            <li><%= link_to t(:users), users_url %></li>
                            <li><%= link_to t(:practice_settings), practice_settings_url %></li>
                        <% end %>
                        <li class="divider"></li>
                        <li><%= link_to t(:profile), current_user %></li>
                        <li class="divider"></li>
                        <li><%= link_to t(:signout), logout_url %></li>
                    </ul>
                </li>
                <li class="hidden-xs hidden-sm">
                    <%= form_tag patients_url, :method => :get do %>
                        <input name="q" class="search" type="text" value="<%= params[:q]%>">
                    <% end %>
                </li>          
            <% end %>
        </ul>
    </nav>        
</header>
<!-- end navbar -->