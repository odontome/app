<div class="row">
	<div class="eight columns">
		<h1><%=_('My practice')%></h1>

		<ul id="list-items">
			<!--<li>
				<h2><%=link_to _("My treatments"), treatments_url %> <span class="label radius blue"><%= @practice.treatments.count %> <%=_('treatments')%></span></h2>
				<span><%=_('Everything that you do to your patients.')%></span>
				
			</li> -->
			<li>
				<h2><%= link_to _("My doctors"), doctors_url %> <span class="label radius blue"><%= @practice.doctors.count %> <%=_('doctors')%></span></h2>
				<span><%=_('The people who do the things to your patients.')%></span>
			</li>
			<li>
				<h2><%=link_to _("My users"), users_url %> <span class="label radius blue"><%= @practice.users.count %> <%=_('users')%></span></h2>
				<span><%=_('Everyone who has access to your odonto.me.')%></span>
			</li>
			<% if @is_user_admin %>
				<li>
					<h2><%=link_to _("My practice settings"), practice_settings_url %></h2>
					<span><%=_('Change here your practice name, select your language and more')%></span>
				</li>
			<% end %>
		</ul>
	</div>
	
	<div class="four columns">
		<div class="panel hide-on-phones">
			<p><%= _("This is where the magic happens, your practice. Although it is magical, it still needs a few settings to run, please spend some seconds in creating your doctors, users, etc.") %></p>
		</div>
	</div>	
</div>
