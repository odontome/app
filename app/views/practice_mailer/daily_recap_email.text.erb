Daily recap for <%= l @date.to_date, :format => :day_and_date %>

<% if !@appointments.nil? %>
<%= @appointments.size %> appointments scheduled
	<% @appointments.each do |appointment| %>
<%= "#{appointment['patient_firstname']} #{appointment['patient_lastname']} with #{appointment['doctor_firstname']} #{appointment['doctor_lastname']} at #{l appointment['starts_at'].to_time, :format => :short}"%>
	<% end %>
<% end %>

<% if !@patients.nil? %>
<%= @patients.size %> patients added
	<% @patients.each do |patient| %>
<%= "#{patient['firstname']} #{patient['lastname']}" %> <%= url_for patient_url(:id => patient['id']) %>
	<% end %>
<% end %>


