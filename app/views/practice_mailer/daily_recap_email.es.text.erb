Resumen del día <%= l @date.to_date, :format => :day_and_date %>

<% if !@appointments.nil? %>
<%= @appointments.size %> citas agendadas
	<% @appointments.each do |appointment| %>
<%= "#{appointment['patient_firstname']} #{appointment['patient_lastname']} con #{appointment['doctor_firstname']} #{appointment['doctor_lastname']} el #{l(appointment['starts_at'].to_time, :format => :short)}"%>
	<% end %>
<% end %>

<% if !@patients.nil? %>
<%= @patients.size %> nuevos pacientes
	<% @patients.each do |patient| %>
<%= "#{patient['firstname']} #{patient['lastname']}" %> <%= url_for patient_url(:id => patient['id']) %>
	<% end %>
<% end %>


