$("<%=escape_javascript(render :partial => 'show', :locals => {:note => @note, :noteable => @noteable})%>").prependTo(".comment").addClass("animated fadeInUp");

// Clear the form
$('#note_notes').val("");