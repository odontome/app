<div id="note<%=note.id%>" class="alert-box">
	<p><span class="radius label blue"><%=l note.created_at, :format => :long %></span>
	<%=note.notes%></p>
	<%= link_to "×", [noteable, note], :method => :delete, :remote => true, :class => "close", :title => t(:delete) %>
</div>