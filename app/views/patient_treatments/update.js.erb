<% if params[:patient_treatment][:is_completed] != nil %>
	// For some reason I need to pass them one by one
	// $('li#treatment<%=@treatment.id%>').toggleClass("checked");
	
	// Toggle the completed icon
	$('li#treatment<%=@treatment.id%> > h2 > span').attr("data-icon", "<%=@treatment.is_completed? == true ? '#' : '' %>");
	
	// Do something to indicate the balance was updated, maybe a sound
<% end %>