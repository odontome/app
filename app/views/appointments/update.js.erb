<% if params[:type] != "drop" && params[:type] != "resize" %>
	window.odontomeCalendar && window.odontomeCalendar.refetchEvents();
	document.querySelectorAll('.modal.show').forEach(function (element) {
		tabler.Modal.getOrCreateInstance(element).hide();
	});
<% end %>
